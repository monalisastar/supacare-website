// Auth is now handled by Supabase.
// This stub prevents legacy imports of @next-auth/prisma-adapter / @prisma/client
// from causing __dirname errors in the serverless runtime.

export const authOptions = {}

export const getSession = async () => null
